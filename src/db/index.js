'use strict'

/**
 * Expose db models in this fashion to support multiple
 * main documents.
 */
module.exports = {
  userDocument: require('./models/userDocument'),
  // AnotherDocument: require('./model/anotherdocument')
}